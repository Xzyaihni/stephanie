used_tiles = {"grass"}

function generate(neighbors, chunk)
    chunk[1] = tilemap["grass"]
    --for i = 1, chunk_size
    --do
    --    chunk[i] = tilemap["grass"]
    --end

    --return chunk
end